@{
    ViewData["Title"] = "Export Demo";
}

<h2>Export Demo - Download Excel dan PDF</h2>

<p>
    <a class="btn btn-success" href="@Url.Action("DownloadExcel")">Download Excel</a>
    <a class="btn btn-primary" href="@Url.Action("DownloadPdf")">Download PDF</a>
</p>
